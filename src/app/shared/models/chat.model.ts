export class Chat {
  groupName: string;
  createdBy ?: string;
  users ?: [string];
  originalLanguage ?: string;
  language ?: string;
  originalText ?: string;
  text ?: string;
  time ?: string;
}
